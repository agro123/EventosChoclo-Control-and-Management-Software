node_js:
  - '14'
dist: trusty
cache:
  directories:
    - $HOME/.cache/yarn
    - node_modules
    - .next/cache
script:
  - npm install
  - npm run build
  - npm run test

  
deploy:
  provider: heroku
  api_key: $APIKEYHEROKU
  app: EventosChoclo